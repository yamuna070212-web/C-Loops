#include <stdio.h>

int main()
{
    int n, i, sum = 0;

    printf("Enter N: ");
    scanf("%d", &n);

    for(i = 2; i <= n; i += 2)
    {
        sum += i;
    }

    printf("Sum of Even Numbers = %d", sum);

    return 0;
}
Sample Output
Enter N: 10
Sum of Even Numbers = 30
